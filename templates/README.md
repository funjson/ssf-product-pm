# templates 目录说明

`templates/` 只定义输出文档结构，不承载阶段规则。

阶段规则在 `flows/`，状态协议在 `core/`，文档和模板索引在 `registries/`。

目录分层：

- `common/`：公共入口模板。
- `analysis/`：分析阶段输出模板。
- `design/`：设计阶段输出模板。
- `state/`：工作区和实例状态资产模板。
