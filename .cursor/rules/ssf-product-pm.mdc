---
description: Use ssf-product-pm when generating AI-ready product analysis, product architecture, PRD, feature task specs, UI specs, prototype prompts, UI annotations, baselines, prototype-input packages, or change specs for AI-driven software development.
globs:
  - "**/*.md"
alwaysApply: false
---

# ssf-product-pm Cursor Rule

When this rule applies, read `SKILL.md` first and follow its runtime load path.

Do not load all references or all templates by default. Load:

1. Required core and registry files named in `SKILL.md`.
2. The current action/document flow.
3. The current output template.
4. Only the reference files needed by the current review, repair, ID, or QA task.

Runtime invariants:

- Generation, modification, repair, continuation, rerun, change, and prototype-run tasks are write-required unless the user explicitly asks for read-only review or no file changes.
- Only `analysis-human-review` and `product-architecture-human-review` may stop for user confirmation.
- Other review gates are automatic: pass continues; fail enters repair-run.
- `registries/documents.md` is the source of truth for document path, flow, template, gate, and interrupt behavior.
- `prototype-run` writes only to target instance `prototype-input/` and must not modify `product-spec/`.
